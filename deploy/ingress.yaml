apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sprawunki
spec:
  rules:
  - http:
      paths:
      - path: "/szuflada"
        pathType: Prefix
        backend:
          service:
            name: szuflada
            port:
              number: 80
